

## Добавление юридических ссылок в Footer

### Что будет сделано

Ссылки на «Политику конфиденциальности» и «Согласие на обработку персональных данных» будут размещены только в подвале сайта (Footer).

### Детали

**1. Две новые страницы** (файлы `src/pages/PrivacyPolicy.tsx` и `src/pages/Consent.tsx`)

Каждая страница будет обёрнута в компонент `Layout` (как остальные внутренние страницы) и содержать полный юридический текст с данными:
- ИП Гантамирова Зарема Джабраиловна
- ИНН: 770301245304
- Адрес: Москва, ул. Новослободская, д. 3, 3 этаж (БЦ Сафа)
- Телефон: +7 985 053 4761
- Email: info@novobeauty.ru

**2. Ссылки в Footer** (`src/components/Footer.tsx`)

Под строкой копирайта добавятся две текстовые ссылки:
- «Политика конфиденциальности» -> `/privacy-policy`
- «Согласие на обработку ПД» -> `/consent`

**3. Ссылки в Landing-футере** (`src/pages/Landing.tsx`)

Лендинг использует свой собственный футер (не общий компонент Footer). В нём тоже появятся эти ссылки рядом с копирайтом.

**4. Маршруты** (`src/App.tsx`)

Два новых роута: `/privacy-policy` и `/consent`.

### Изменяемые файлы
- Создание: `src/pages/PrivacyPolicy.tsx`, `src/pages/Consent.tsx`
- Редактирование: `src/components/Footer.tsx`, `src/pages/Landing.tsx`, `src/App.tsx`

